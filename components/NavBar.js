import React from "react";

const NavBar = () => {
  return (
    <nav className="flex flex-wrap items-center justify-between text-xl text-gray-200">
      <div>
        <a href="/" className="font-semibold tracking-tight">
          compti.me
        </a>
      </div>
    </nav>
  );
};

export default NavBar;
